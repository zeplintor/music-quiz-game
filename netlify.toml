[build]
  command = "npm run build"
  publish = "dist"
  ignore = "backend/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--no-optional"
